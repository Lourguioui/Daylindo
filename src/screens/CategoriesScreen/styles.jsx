import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 16,
    paddingHorizontal: 8,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
});

export default styles;
